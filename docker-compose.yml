services:
  api:
    build: .
    environment:
      DATABASE_URL: ${DATABASE_URL:-postgres://postgres:postgres@host.docker.internal:5432/postgres?sslmode=disable}
      PORT: "8080"
    ports:
      - "8080:8080"
    command: ["./server"]

